import { ShippingAddressGuard } from './shipping-address.guard';

describe('ShippingAddressGuard', () => {
  it('should be defined', () => {
    expect(new ShippingAddressGuard()).toBeDefined();
  });
});
